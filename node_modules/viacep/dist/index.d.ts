import { Address, State } from './models';
export * from './models';
export declare class ViaCEP {
    private readonly request;
    constructor();
    cep(cep: string): Promise<Address>;
    cep({ state, city, street, }: {
        state: State;
        city: string;
        street: string;
    }): Promise<Address[]>;
}
